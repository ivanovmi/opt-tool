packages:
  - name: glance
    components:
      - api:
        - version: 1
          entity:
            - controller:
                - get_image_meta
                - get_active_image_meta
            - images
            - members
            - router
        - version: 2
          entity:
            - images
            - router
      - db:
      - common:
      - registry:
  - name: python-glanceclient
    components:
      - glanceclient:
        - version: 1
          entity:
            - image_members
            - images
output_dir: /tmp/test_opt_tool
